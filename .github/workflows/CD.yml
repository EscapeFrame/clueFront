name: React CI/CD

on:
  pull_request:
    branches: [ develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 리포지토리 체크아웃
      - name: Checkout
        uses: actions/checkout@v3

      # 2️⃣ Node.js 환경 설정 (React 빌드를 위해)
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # 3️⃣ 의존성 설치
      - name: Install dependencies
        run: npm ci

      # 4️⃣ React 앱 빌드
      - name: Build React app
        run: npm run build

      # 5️⃣ 배포 파일(zip) 생성
      - name: Make zip file
        run: zip -qq -r ./$GITHUB_SHA.zip dist appspec.yml scripts/

      # 6️⃣ AWS 자격 증명 설정
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ACCESS_KEY_SECRET }}
          aws-region: ap-northeast-2

      # 7️⃣ S3 업로드
      - name: Upload to S3
        run: aws s3 cp ./$GITHUB_SHA.zip s3://{버킷이름}/$GITHUB_SHA.zip

      # 8️⃣ CodeDeploy 배포 요청
      - name: Code Deploy
        run: |
          aws deploy create-deployment \
            --application-name {애플리케이션 이름} \
            --deployment-group-name {배포 그룹 이름} \
            --deployment-config-name CodeDeployDefault.AllAtOnce \
            --s3-location bucket={버킷이름},bundleType=zip,key=$GITHUB_SHA.zip
