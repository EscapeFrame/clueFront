/*

import Customapi from '@/shared/config/api';
import { Exam } from '@/shared/types/class/'; // 아직 업어요..

export const examApi = {
  // 시험 목록 조회
  fetchExams: async (examNumber: string): Promise<Exam[]> => {
    const res = await Customapi.get<Exam[]>(`/api/exam/${examNumber}`);
    return res.data;
  }
};

*/